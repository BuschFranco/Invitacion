---
---

<div id="rsvp-section" class="rsvp-section">
    <h2>Registro de asistencia</h2>
    <p>Es muy importante para mí que puedas confirmar lo antes posible si vas a poder asistir y acompañarme, y si tienes alguna restricción alimentaria.</p>
    <div class="rsvp-form">
        <input type="text" id="nombre" placeholder="Tu nombre" class="rsvp-input">
        <input type="number" id="invitados" placeholder="Número de invitados" class="rsvp-input">
        <textarea id="restricciones" placeholder="¿Tienes alguna restricción alimentaria?" class="rsvp-input"></textarea>
        <button class="rsvp-button" id="confirmar-btn">Confirmar Asistencia</button>
    </div>
    <div id="confirmacion-mensaje" class="confirmacion-mensaje hidden">
        <i class="fa-solid fa-heart"></i>
        <p>¡Gracias por confirmar tu asistencia!</p>
    </div>
</div>

<script>
    // Manejar el envío del formulario de confirmación
    document.addEventListener('DOMContentLoaded', function() {
        const confirmarBtn = document.getElementById('confirmar-btn');
        const confirmacionMensaje = document.getElementById('confirmacion-mensaje');
        const nombreInput = document.getElementById('nombre');
        const invitadosInput = document.getElementById('invitados');
        const restriccionesInput = document.getElementById('restricciones');
        const rsvpForm = document.querySelector('.rsvp-form');

        if (confirmarBtn) {
            confirmarBtn.addEventListener('click', function() {
                // Validación básica
                if (!nombreInput.value.trim()) {
                    alert('Por favor, ingresa tu nombre.');
                    return;
                }

                if (!invitadosInput.value || invitadosInput.value < 1) {
                    alert('Por favor, indica el número de invitados.');
                    return;
                }

                // Aquí podrías enviar los datos a un servidor si fuera necesario
                console.log('Confirmación recibida:', {
                    nombre: nombreInput.value,
                    invitados: invitadosInput.value,
                    restricciones: restriccionesInput.value
                });

                // Mostrar mensaje de confirmación
                rsvpForm.style.display = 'none';
                confirmacionMensaje.classList.remove('hidden');
                confirmacionMensaje.style.display = 'block';
                setTimeout(() => {
                    confirmacionMensaje.style.opacity = '1';
                    confirmacionMensaje.style.transform = 'translateY(0)';
                }, 10);
            });
        }
    });
</script>

<style>
    .rsvp-section {
        margin: 2rem 0;
    }

    .rsvp-section h2 {
        font-size: 2rem;
        color: var(--primary-color);
        margin-bottom: 1rem;
    }

    .rsvp-section p {
        font-size: 1.2rem;
        margin-bottom: 1.5rem;
        max-width: 800px;
        margin-left: auto;
        margin-right: auto;
    }

    .rsvp-form {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        max-width: 500px;
        margin: 0 auto;
        padding: 1rem;
    }

    .rsvp-input {
        padding: 0.8rem 1rem;
        border: 2px solid var(--secondary-color);
        border-radius: 8px;
        font-family: inherit;
        font-size: 1rem;
        transition: border-color 0.3s ease;
    }

    .rsvp-input:focus {
        border-color: var(--accent-color);
        outline: none;
    }

    textarea.rsvp-input {
        min-height: 100px;
        resize: vertical;
    }

    .rsvp-button {
        background-color: var(--accent-color);
        color: white;
        border: none;
        padding: 0.8rem 2rem;
        font-size: 1.2rem;
        border-radius: 30px;
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.2s ease;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        margin-top: 0.5rem;
    }

    .rsvp-button:hover {
        background-color: #e60073;
        transform: translateY(-2px);
        box-shadow: 0 6px 8px rgba(0, 0, 0, 0.15);
    }

    .confirmacion-mensaje {
        margin-top: 1.5rem;
        padding: 1rem;
        background-color: rgba(255, 255, 255, 0.8);
        border-radius: 10px;
        text-align: center;
        transition: opacity 0.5s ease, transform 0.5s ease;
    }

    .confirmacion-mensaje i {
        font-size: 2rem;
        color: var(--accent-color);
        margin-bottom: 0.5rem;
    }

    .confirmacion-mensaje p {
        font-size: 1.2rem;
        color: var(--accent-color);
        font-weight: bold;
    }

    .hidden {
        display: none;
        opacity: 0;
        transform: translateY(20px);
    }

    /* Media queries para responsividad */
    @media (max-width: 768px) {
        .rsvp-section h2 {
            font-size: 1.8rem;
        }

        .rsvp-section p {
            font-size: 1.1rem;
        }
    }

    @media (max-width: 480px) {
        .rsvp-section h2 {
            font-size: 1.5rem;
        }

        .rsvp-section p {
            font-size: 1rem;
        }

        .rsvp-button {
            padding: 0.6rem 1.5rem;
            font-size: 1rem;
        }
    }
</style>